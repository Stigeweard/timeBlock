{
    "manifest_version": 2,

    "name": "TimeBlock",
    "description": "This extension allows the user to limit the number of times a site is visited per hour.",
    "version": "1.0",

    "browser_action": {
        "default_icon": "icon.png",
        "default_popup": "popup.html"
    },
    "permissions": [
        "activeTab",
        "storage",
        "webRequest"
    ]
}
